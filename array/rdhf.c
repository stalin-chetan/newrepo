#include<stdio.h>
int main()
{
    int n;
    int array[10];
    printf("Enter the value which should be store in array:");
    for(int i=0;i<10;i++)
    {
        scanf("%d",&n);
    }
    for(int i=0; i<=n; i++)
    {
        if(array[i])
        {
            
        }
    }
    return 0;
}