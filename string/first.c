// write a program in c the input the a sentence from the user and check the number of vowel in the sentences

#include <stdio.h>
#include <string.h>
int main()
{
    char sen[101];
    printf("Enter a sentence: \n");
    scanf("%s", sen);

    printf("%s", sen);

    return 0;
}